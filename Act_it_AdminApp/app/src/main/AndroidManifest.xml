<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hoperaiser.act_it_adminapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Act_it_AdminApp">
        <activity android:name=".AttendanceActivity"></activity>
        <activity android:name=".AnnouncementActivity" />
        <activity android:name=".NotesActivity" />
        <activity android:name=".SyllabusActivity" />
        <activity android:name=".Profile_StudentActivity" />
        <activity android:name=".Student_detailsActivit" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TimetableActivity" />
        <activity android:name=".ExpolreActivity" />
        <activity android:name=".DisplayImagesActivity" />
        <activity android:name=".AddBannerActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>